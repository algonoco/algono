name: deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    environment: github-pages

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Inject Gemini API key
        env:
          GEMINI_API_KEY_SECRET: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python - <<'PY'
          from pathlib import Path
          import os

          path = Path("index.html")
          content = path.read_text(encoding="utf-8")
          placeholder = '"GEMINI_API_KEY"'
          replacement = f'"{os.environ["GEMINI_API_KEY_SECRET"]}"'
          occurrences = content.count(placeholder)

          if occurrences != 1:
              raise SystemExit(f"Expected exactly 1 {placeholder} token in index.html, found {occurrences}.")

          content = content.replace(placeholder, replacement, 1)
          path.write_text(content, encoding="utf-8")
          PY

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
